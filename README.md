# Movie Recomendation project

Simple project that uses movielens dataset and gives the best recomendation for a movie based on other users reviews.

## Installation

Install the needed libraries for the project to run:
```bash
pip3 install -r requirements.txt  
```

## Run

```bash
python3 recomender.py
```
